#include<stdio.h>
int fact(int);
int fact(int n)
{
    if(n==1)
    {
        return;
    }
    else
    {
        return n*fact(n-1);
    }
}
void main()
{
    int n;
    printf("enter a number:");
    scanf("%d",&n);
    printf("the factorials are:%d",fact(n));
}
